import { NextResponse } from 'next/server'

export async function POST() {
  // Placeholder for Vapi server-side webhook/handler
  // We will receive audio URLs / transcripts here once creds are added
  return NextResponse.json({ ok: true })
}


